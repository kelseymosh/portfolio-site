import React from 'react'
import { HeroContainer } from './HeroElements'

const HeroSection = () => {
    return (
        <HeroContainer>
            
        </HeroContainer>
    )
}

export default HeroSection

